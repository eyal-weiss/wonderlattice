{
  "name": "wonderloom",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs . 4173",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/unit/",
    "test:browser": "playwright test",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "npm run lint && npm run format:check && npm test && npm run build && npm run test:browser"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.63.0",
    "eslint": "^10.11.0",
    "globals": "^17.12.0",
    "prettier": "^3.9.9"
  },
  "description": "A small, dependency-free playground for beautiful mathematical ideas.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
